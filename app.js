let i;          //「変数i」を宣言
let num = 0;    //「変数num」を宣言し、初期値を「0」に設定

for(i=1; i<11; i=i+1){  //変数iの初期値を「1」に設定し、変数iが「11」未満のときは変数iに1を足す（「i++」は「i = i+1」と同じ）
    num = num+i;       //変数numに「num+i」を代入
}

alert('1から10まで足し算した結果は' + num + 'です');